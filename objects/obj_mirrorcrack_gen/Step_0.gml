if (nodes >= max_nodes)
{
    var complete = 0
    with (obj_mirrorcrackattack)
        complete += finished
    if (complete >= max_nodes)
    {
        global.turntimer = 0
        instance_destroy()
    }
}
